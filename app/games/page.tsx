import { GamesScreen } from "@/features/games/screens/GamesScreen";

export const metadata = {
  title: "Framely — choose a game",
};

const GamesPage = () => <GamesScreen />;

export default GamesPage;
